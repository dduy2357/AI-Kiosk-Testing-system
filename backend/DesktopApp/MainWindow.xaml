<Window x:Class="DesktopApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:local="clr-namespace:DesktopApp"
        mc:Ignorable="d"
          WindowStyle="None" WindowState="Maximized">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <local:TopNavBar x:Name="TopNavBarControl" Grid.Row="0" Background="LightGray" Margin="0 5 0 0" />

        <!-- WebView2 chiếm toàn bộ không gian trên -->
        <wv2:WebView2 x:Name="WebView"
                      Grid.Row="1"
                      Margin="0"
                      Visibility="Visible"/>

        <!-- Thanh tab ở dưới cùng -->
        <local:BottomTray Grid.Row="2"
                          Background="LightGray"
                            ExitButtonClick="BottomTray_ExitButtonClick"/>
    </Grid>
</Window>